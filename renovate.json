{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track the pinned ComplianceAsCode/content release in config/content.yaml",
      "managerFilePatterns": [
        "/^config/content\\.yaml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+) extractVersion=(?<extractVersion>\\S+)\\s+version:\\s*\"(?<currentValue>[^\"]+)\""
      ]
    },
    {
      "customType": "regex",
      "description": "Track the pinned ignition-validate release used by the payload checks",
      "managerFilePatterns": [
        "/^\\.github/workflows/ci\\.yaml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+IGNITION_VERSION:\\s*(?<currentValue>\\S+)"
      ]
    },
    {
      "customType": "regex",
      "description": "Track the pinned helm-unittest plugin version in the workflows",
      "managerFilePatterns": [
        "/^\\.github/workflows/.+\\.yaml$/"
      ],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+HELM_UNITTEST_VERSION:\\s*(?<currentValue>\\S+)"
      ]
    }
  ],
  "packageRules": [
    {
      "description": "Automerge GitHub Actions dependency updates once CI is green",
      "matchManagers": [
        "github-actions"
      ],
      "automerge": true
    },
    {
      "matchDepNames": [
        "ComplianceAsCode/content"
      ],
      "groupName": "compliance content",
      "commitMessageTopic": "ComplianceAsCode/content",
      "prBodyNotes": [
        "After merge, refresh the pinned checksum and regenerate the charts:",
        "```sh",
        "make update-sha   # rewrites config/content.yaml sha512 for the new version",
        "make generate     # regenerates charts/ and RULES.md",
        "make verify       # lint + unit tests",
        "```",
        "Review the diff of `charts/` and `RULES.md`: it shows exactly which rules and fixes changed between content releases."
      ]
    }
  ]
}
