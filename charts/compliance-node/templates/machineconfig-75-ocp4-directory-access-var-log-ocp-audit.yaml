{{- define "cr.frag.machineconfig-75-ocp4-directory-access-var-log-ocp-audit.ocp4-directory-access-var-log-ocp-audit.0" -}}
spec:
  config:
    ignition:
      version: 3.1.0
    storage:
      files:
      - contents:
          source: data:,-a%20always%2Cexit%20-F%20dir%3D/var/log/openshift-apiserver/%20-F%20perm%3Dr%20-F%20auid%3E%3D1000%20-F%20auid%21%3Dunset%20-F%20key%3Daccess-audit-trail%0A
        mode: 0600
        path: /etc/audit/rules.d/30-access-var-log-ocp-audit.rules
        overwrite: true
{{- end -}}

{{- /* MachineConfig/75-ocp4-directory-access-var-log-ocp-audit - rule(s): ocp4-directory_access_var_log_ocp_audit */ -}}
{{- if and (.Values.node.enabled) (eq (include "cr.anyActive" (dict "root" . "rules" (list "ocp4-directory_access_var_log_ocp_audit"))) "true") -}}
{{- $merged := dict -}}
{{- if eq (include "cr.ruleActive" (dict "root" . "rule" "ocp4-directory_access_var_log_ocp_audit")) "true" -}}
{{- $frag := include "cr.frag.machineconfig-75-ocp4-directory-access-var-log-ocp-audit.ocp4-directory-access-var-log-ocp-audit.0" . | fromYaml -}}
{{- $merged = mustMergeOverwrite $merged $frag -}}
{{- end -}}
{{- if not $merged -}}
{{- fail (printf "No remediation applies to MachineConfig/75-ocp4-directory-access-var-log-ocp-audit at cluster.ocpVersion %s. Every fix variant of the active rule(s) (ocp4-directory_access_var_log_ocp_audit) is constrained to a different OpenShift version. Disable the rule(s) or set a supported cluster.ocpVersion." (.Values.cluster.ocpVersion | toString)) -}}
{{- end -}}
{{- range $role := (.Values.node.roles | uniq) }}
---
apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfig
metadata:
  name: {{ printf "%s-%s" "75-ocp4-directory-access-var-log-ocp-audit" $role }}
  labels:
    app.kubernetes.io/managed-by: {{ $.Release.Service | quote }}
    compliance.openshift.io/managed: "true"
    machineconfiguration.openshift.io/role: {{ $role | quote }}
{{ $merged | toYaml }}
{{- end }}
{{- end -}}
