{{- define "cr.frag.kubeletconfig-compliance-kubelet-enable-protect-kernel-defaults.ocp4-kubelet-enable-protect-kernel-defaults.0" -}}
spec:
  kubeletConfig:
    protectKernelDefaults: true
{{- end -}}

{{- /* KubeletConfig/compliance-kubelet-enable-protect-kernel-defaults - rule(s): ocp4-kubelet_enable_protect_kernel_defaults */ -}}
{{- if and (.Values.node.enabled) (eq (include "cr.anyActive" (dict "root" . "rules" (list "ocp4-kubelet_enable_protect_kernel_defaults"))) "true") -}}
{{- $merged := dict -}}
{{- if eq (include "cr.ruleActive" (dict "root" . "rule" "ocp4-kubelet_enable_protect_kernel_defaults")) "true" -}}
{{- $frag := include "cr.frag.kubeletconfig-compliance-kubelet-enable-protect-kernel-defaults.ocp4-kubelet-enable-protect-kernel-defaults.0" . | fromYaml -}}
{{- $merged = mustMergeOverwrite $merged $frag -}}
{{- end -}}
{{- range $role := (.Values.node.roles | uniq) }}
---
apiVersion: machineconfiguration.openshift.io/v1
kind: KubeletConfig
metadata:
  name: {{ printf "%s-%s" "compliance-kubelet-enable-protect-kernel-defaults" $role }}
  labels:
    app.kubernetes.io/managed-by: {{ $.Release.Service | quote }}
    compliance.openshift.io/managed: "true"
    machineconfiguration.openshift.io/role: {{ $role | quote }}
{{ $merged | toYaml }}
{{- end }}
{{- end -}}
