{{- define "cr.frag.machineconfig-75-ocp4-disable-ctrlaltdel-burstaction.rhcos4-disable-ctrlaltdel-burstaction.0" -}}
spec:
  config:
    ignition:
      version: 3.1.0
    storage:
      files:
      - contents:
          source: data:,CtrlAltDelBurstAction%3Dnone
        mode: 0644
        path: /etc/systemd/system.conf.d/disable_ctrlaltdelete_burstaction.conf
        overwrite: true
{{- end -}}

{{- /* MachineConfig/75-ocp4-disable-ctrlaltdel-burstaction - rule(s): rhcos4-disable_ctrlaltdel_burstaction */ -}}
{{- if and (.Values.node.enabled) (eq (include "cr.anyActive" (dict "root" . "rules" (list "rhcos4-disable_ctrlaltdel_burstaction"))) "true") -}}
{{- $merged := dict -}}
{{- if eq (include "cr.ruleActive" (dict "root" . "rule" "rhcos4-disable_ctrlaltdel_burstaction")) "true" -}}
{{- $frag := include "cr.frag.machineconfig-75-ocp4-disable-ctrlaltdel-burstaction.rhcos4-disable-ctrlaltdel-burstaction.0" . | fromYaml -}}
{{- $merged = mustMergeOverwrite $merged $frag -}}
{{- end -}}
{{- if not $merged -}}
{{- fail (printf "No remediation applies to MachineConfig/75-ocp4-disable-ctrlaltdel-burstaction at cluster.ocpVersion %s. Every fix variant of the active rule(s) (rhcos4-disable_ctrlaltdel_burstaction) is constrained to a different OpenShift version. Disable the rule(s) or set a supported cluster.ocpVersion." (.Values.cluster.ocpVersion | toString)) -}}
{{- end -}}
{{- range $role := (.Values.node.roles | uniq) }}
---
apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfig
metadata:
  name: {{ printf "%s-%s" "75-ocp4-disable-ctrlaltdel-burstaction" $role }}
  labels:
    app.kubernetes.io/managed-by: {{ $.Release.Service | quote }}
    compliance.openshift.io/managed: "true"
    machineconfiguration.openshift.io/role: {{ $role | quote }}
{{ $merged | toYaml }}
{{- end }}
{{- end -}}
